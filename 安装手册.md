# 消费记录助手 · 安装手册

**版本**：v0.4.2 ｜ **适用思源版本**：v3.0.0 及以上（在 v3.8.4 上开发验证）

一个思源笔记插件：定时扫描收集箱中的图片与文字，通过 AI 自动识别为消费记录（金额、科目、时间、地点、商家、备注），按消费日期登记到每日文档（如 `支出记录/20260924`）中的独立数据库表格，并自动生成当日汇总（总支出 + 科目占比）。还可选接入企业微信智能机器人，在企微里发消息即可收集。

---

## 一、环境要求

| 项目 | 要求 |
|---|---|
| 思源笔记 | v3.0.0+（桌面版） |
| AI 接口 | 智谱开放平台免费模型（推荐），或任何 OpenAI 兼容接口（OpenAI、SiliconFlow、one-api 中转等） |
| 企业微信采集（可选） | 企业微信管理后台权限，可创建「智能机器人」 |

**申请智谱 API Key（免费）**：注册 [open.bigmodel.cn](https://open.bigmodel.cn) → 控制台 →「API 密钥」→ 创建。插件默认使用 `glm-4-flash`（文字识别）和 `glm-4v-flash`（图片识别），两者均为免费模型，个人记账的低频调用完全够用。

---

## 二、安装插件

1. 解压 `siyuan-plugin-expense-recorder-0.4.2.zip`，得到文件夹 `siyuan-plugin-expense-recorder`，内含：
   ```
   siyuan-plugin-expense-recorder/
   ├── plugin.json      ← 插件清单
   ├── index.js         ← 插件代码
   ├── icon.png         ← 图标
   ├── README.md        ← 功能说明
   └── i18n/            ← 中英文界面文案
   ```
2. 把整个文件夹放入思源工作空间的插件目录：
   ```
   {思源工作空间}/data/plugins/siyuan-plugin-expense-recorder/
   ```
   工作空间位置可在思源「设置 → 关于 → 工作空间路径」查看（Windows 默认 `C:\Users\{用户名}\SiYuan`）。
3. **重启思源**。
4. 在「设置 → 插件」（或「设置 → 集市 → 已下载」）中启用**「消费记录助手」**。
5. 启用成功后，窗口顶栏会出现一个**钱包图标**。

> ⚠️ 重要：以后更新插件文件（覆盖 index.js 等）后，必须**完整重启思源**才会生效，热重载不可靠。

---

## 三、基础配置（必做）

点击「设置 → 插件 → 消费记录助手」右侧的**设置图标**，填写：

| 配置项 | 说明 | 建议值 |
|---|---|---|
| 收集箱笔记本 | 要扫描的笔记本 | 专建一个，或用现有的 |
| 仅处理该路径下的文档 | 缩小扫描范围 | 如 `/原始单据`（留空则扫整个笔记本） |
| 登记消费记录的笔记本 | 每日文档所在笔记本 | 如 `家庭开支` |
| 日记文档路径模板 | `{date}` 替换为 `20260924` | `/支出记录/{date}` |
| AI 接口地址 | OpenAI 兼容 Base URL | 默认智谱，无需改 |
| API Key | 按上文申请 | 必填 |
| 文本 / 图片识别模型 | | 默认 `glm-4-flash` / `glm-4v-flash`，无需改 |
| 科目列表 | 「、」分隔 | 按自己的消费习惯增删 |
| 定时处理间隔 | 分钟 | 默认 30 |

填好后依次点击：

1. **「测试 AI 连接」**——应提示连接成功；
2. **「保存」**；
3. 往收集箱贴一张支付截图或一段文字，点**顶栏钱包图标**（或命令面板执行「立即处理收集箱」），检查 `支出记录/当日` 文档是否生成了记录。

---

## 四、企业微信智能机器人采集（可选）

在手机/电脑上把消费凭证发给企微机器人，插件自动收进收集箱：

1. 登录[企业微信管理后台](https://work.weixin.qq.com) → 【协作】→【智能机器人】→ 创建机器人；
2. **连接方式选择「长连接」**（无需公网服务器）；
3. 把机器人的 **Bot ID** 和 **Secret** 填入插件设置，勾选「启用企业微信智能机器人采集」；
4. 把机器人添加进**内部群**（群里 **@机器人** 发消息），或直接**单聊**机器人发送。

支持文字、图片、语音（自动转文字）、文件；图片/文件自动下载解密后存为思源本地资产。可在设置里填自己的 userid，只采集自己发的消息。

**注意**：
- 智能机器人只能进**内部群**（客户群/外部群不支持），群聊中仅 @机器人 的消息会被收到；
- **思源未运行期间的消息不会补收**；
- Secret 属于敏感凭据，若曾在不安全渠道暴露，请到企微后台重置后更新插件配置；
- 连接与采集状态可查看 `data/storage/petal/siyuan-plugin-expense-recorder/wecom-last.json`。

---

## 五、数据库表格登记（可选，推荐）

插件设置中勾选「**启用数据库表格登记**」即可，无需其他配置：

- 每天在当日文档中自动创建一张**独立数据库表格**（列：金额/科目/日期/时间/地点/商家/备注，全部自动创建）；
- 表格上方自动生成并实时更新汇总：`当日总支出：¥xxx（共 N 笔）` 与各科目金额占比；
- **自动去重**：按「时间+金额」等指纹拦截重叠截图、重发消息造成的重复登记；
- 删除当日文档后，下次登记会自动重建（表格数据不丢失）。

不勾选时，记录以文字列表形式登记到当日文档。

---

## 六、日常使用

```
手机上看到支付截图/小票
   ↓ 发给企微机器人（或手动贴进思源收集箱文档）
插件定时扫描（默认 30 分钟，也可点顶栏钱包图标立即处理）
   ↓ AI 识别：金额、科目、时间、地点、商家、备注
按消费日期写入对应每日文档的数据库表格
   ↓
汇总自动更新（当日总支出 + 科目占比）
```

**识别规则要点**：
- 内容里没有明确日期时按**当天**登记（不会编造日期）；
- 支付宝/微信账单中的**负数支出**自动转为正数；
- 账单截图中的**多笔交易**会逐笔拆分登记；
- 与消费无关的内容会被跳过并标记已处理，不会反复识别。

---

## 七、常见问题

**Q：插件没反应/结果异常，怎么排查？**
每轮处理的完整结果写在 `data/storage/petal/siyuan-plugin-expense-recorder/lastrun.json`（扫描数、登记数、去重数、失败原因）；企微连接看同目录 `wecom-last.json`。更详细的失败堆栈在思源开发者工具（`Ctrl+Shift+I`）控制台，关键字 `[expense-recorder]`。

**Q：测试 AI 连接报 Failed to fetch / CORS？**
该接口不允许浏览器直连，换智谱/OpenAI 官方接口，或自建 one-api 中转后把地址填为中转地址。

**Q：更新插件后行为没变化？**
完整重启思源（直接退出进程再打开，不要只重载界面）。

**Q：识别科目不准？**
把你的常用科目补进设置的科目列表，模型会优先从列表归类；仍不满意可换更强的模型（如 `glm-4.5v`）。

**Q：机器人收不到消息？**
确认是内部群且消息 @ 了机器人（单聊无需 @）；确认思源处于运行状态；查看 `wecom-last.json` 的 `lastError`（订阅失败通常是 Bot ID/Secret 不对）。

**Q：数据存在哪里？如何备份？**
- 消费记录：思源数据库表格（`data/storage/av/`），随工作空间同步/备份；
- 插件配置与运行状态：`data/storage/petal/siyuan-plugin-expense-recorder/`。
- 整个工作空间用思源自带的同步/备份即可覆盖全部数据。

**Q：隐私？**
待处理的文字与图片会发送给你配置的 AI 服务商做识别，除此之外不向任何第三方发送数据；企微 Secret 与 API Key 只保存在本机插件数据目录。

---

## 八、升级与卸载

- **升级**：用新包内容覆盖 `data/plugins/siyuan-plugin-expense-recorder/` 下的同名文件，重启思源。配置和历史数据不受影响。
- **卸载**：思源「设置 → 插件」中停用，删除上述文件夹即可。已登记的消费记录保留在笔记中。

---

## 九、开发者说明（可选阅读）

源码包 `siyuan-plugin-expense-recorder-source-0.4.2.zip`（TypeScript + Vite）：

```bash
npm install
npm run dev      # 监听构建
npm run build    # 构建到 dist/
npm run check    # 类型检查
npm run smoke    # 核心逻辑冒烟测试
node scripts/mock-wecom-bot.mjs [port] [图片] [文件]   # 本地模拟企微机器人做端到端测试
```

代码结构：`src/index.ts`（入口/定时器）、`processor.ts`（扫描→识别→登记流程）、`recognizer.ts`（AI 调用与解析）、`wecom.ts`（企微长连接客户端）、`settings.ts`（设置界面）、`api.ts`（思源内核 API 封装）。
